# 더망고 신규상품수집 — 저장상품수 일괄변경 (사용법)

`더망고_저장상품수_일괄변경.user.js` 는 신규상품수집 목록에서 **검색된 필터들의 "저장상품수(limit_count)"를 지정값(기본 100)으로 일괄 변경**하는 유저스크립트입니다. (필터 생성 시 3개로 만든 뒤, 본수집 전에 100개(또는 200개)로 올리는 단계를 자동화)

## 작동 방식 (빠르고 안전)
- **팝업·새로고침·페이지 이동 없이** 순수 fetch로 처리합니다.
- 각 필터의 수정폼을 읽어 **저장상품수만 바꾸고 나머지 설정(카테고리 매핑·번역·정책 등)은 그대로** POST 저장합니다.
- 정적 파싱 값이 실제 렌더 값과 100% 동일함을 확인했고, 저장 후 다른 필드가 보존됨을 검증했습니다.

## 설치 (Tampermonkey)
1. Tampermonkey → **새 스크립트 만들기**
2. `더망고_저장상품수_일괄변경.user.js` 내용을 **전체 붙여넣기** → **Ctrl+S**

## 사용
1. 더망고 관리자 로그인 상태에서 `상품데이터수집 → 신규상품 수집`(getGoodsCategory.php)으로 이동.
2. **페이지 위쪽 검색폼에서 대상을 좁히고 `선택조건으로 검색하기`를 누릅니다.** 화면에 나온 그 목록이 그대로 변경 대상입니다.
3. 우측 상단 **파란 패널**에서:
   - **저장상품수**: 목표값 입력(기본 100. 3만개 업그레이드 시 200).
   - **현재 조회조건** 박스에 지금 적용된 조건이 요약돼 있습니다. 조건이 하나도 없으면 **빨간 글씨로 `없음 — 전체 필터`** 라고 뜹니다.
4. **`대상 조회`** 를 누르면 아무것도 바꾸지 않고 **대상 건수만** 먼저 확인합니다.
5. **`시작`** → 조건과 건수가 담긴 확인창이 한 번 뜨고, 확인하면 하나씩 변경합니다. 진행 상황이 패널에 표시됩니다.
6. **`정지`** 버튼으로 중단 가능(조회 단계에서도 멈춥니다).

## ★ 대상 지정 — v1.3부터 페이지 검색조건을 그대로 따릅니다

패널에 검색항목을 따로 두지 않습니다. **매크로는 지금 URL에 실려 있는 검색 파라미터를 그대로 재사용**합니다. 검색폼이 GET이라 URL이 곧 화면 상태이므로 **"화면에 보이는 목록 = 바뀔 대상"** 이 항상 성립하고, 더망고가 검색항목을 추가해도 코드를 고칠 필요가 없습니다.

쓸 수 있는 조건(전부 실측 확인):

| 화면 항목 | 파라미터 | 비고 |
|---|---|---|
| 날짜검색 **체크박스** | `ps_duse=1` | ★ **이 체크박스를 켜지 않으면 날짜가 통째로 무시됩니다** — 날짜만 지정하면 전체 결과와 동일합니다. 패널 요약도 이 체크가 없으면 날짜를 표시하지 않습니다 |
| 최근수집일 / 필터생성일 | `date_type` = `modify` / **빈값** | 필터생성일이 빈값입니다 |
| 기간 | `start_yy/mm/dd`, `end_yy/mm/dd` | |
| 수집사이트 | `site_id` | `a_rt` / `zara_de` / `zara_kr` |
| 매출발생 여부 | `sales_yn` | `Y` 매출 있음 / `N` 매출 없음 |
| 검색필터명 키워드 | `sch_keyword` | 부분일치 |
| 필터 보기 유형 | `ft_show` | 빈값 / `saved` 상품이 저장된 필터만 / `sales` 매출이 발생한 필터만 |
| 정렬 | `ft_sort` | 대상 집합을 바꾸지 않으므로 요약에서 생략 |

- 매크로가 **모르는 파라미터는 요약에 `이름=값` 원문으로 그대로 표시**됩니다. 화면에 안 보이는 조건이 조용히 적용되는 일은 없습니다.
- `pg`·`ft_num`은 매크로가 열거하면서 직접 정하므로 무시합니다.
- `pmode`·`uids`·`chk_value`·`all_chk`는 **목록 선택·삭제용 필드**라 잘라냅니다. 페이지의 검색 버튼(`search_filter()`)이 폼을 통째로 GET 제출하면서 `pmode`를 비우지 않아 URL에 `pmode=filter_delete`가 섞여 들어오기 때문입니다(조회에는 영향 없음).

⚠️ **조건을 바꿨으면 반드시 페이지에서 `검색`을 눌러야 합니다.** 셀렉트만 건드리고 검색을 누르지 않으면 URL이 그대로라 매크로는 이전 조건으로 동작합니다. 패널의 `현재 조회조건` 박스와 화면 목록이 일치하는지 보면 됩니다.

## 특징
- **이미 목표값(예: 100)인 필터는 자동 건너뜀** → 재실행해도 안전(중단 후 다시 시작하면 남은 것만 처리).
- 실패한 필터가 있으면 패널에 **실패 ID**가 표시됩니다. 다시 "시작"하면 재시도됩니다.
- 완료 메시지에 **적용했던 조건이 함께 남습니다** — 무엇을 바꿨는지 나중에 확인할 수 있게.
- 200개 필터 기준 대략 2~4분.

## 한계 / 유지보수
- 더망고 화면·내부 함수가 바뀌면 수정이 필요할 수 있습니다. 현재 의존 요소:
  - 목록 페이지네이션: `getGoodsCategory.php?ft_num=100&pg=N` (파라미터 `pg`)
  - 검색폼이 **GET**이라는 성질 — 이게 깨지면 조건 연동이 통째로 무너집니다(그때는 `document.fm_search`에서 직접 읽는 방식으로 바꿔야 함)
  - 사이트 이름 표시용으로만 검색폼(`form[name=fm_search]`)의 `select[name=site_id]`를 읽습니다. 못 찾으면 코드값(`a_rt` 등)이 그대로 표시될 뿐 동작에는 지장 없습니다
  - 수정폼 URL: `modify_filter(필터ID)` 의 window.open 대상 (`admin_group_modify.php`, `/mall/admin/`)
  - 저장 endpoint: 수정폼 action = `admin_etc_ok.php` (`/mall/admin/`), 저장상품수 필드 = `limit_count`
