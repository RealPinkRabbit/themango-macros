# 카테고리매핑 규칙 가이드 (파일기반 v2.1 · ② 매핑 단계)

> 이 문서는 **Claude가 `카탈로그+매핑` 엑셀을 받아 `매핑` 시트를 채울 때 지키는 규칙**입니다.
> 규칙이 매크로 코드(정규식)에서 빠져 이 문서로 이동했습니다 → **신상품이 추가돼도 매크로 수정 없이 이 규칙대로 Claude가 판단**합니다.
> **v2.1 변경**: 신발 전용이었던 규칙을 **신발 + 의류** 2종으로 확장. 카탈로그/매핑 시트에 `구분`(신발/의류) 열이 추가됨.

## 입력/출력
- 입력: 엑셀 워크북 3개 시트
  - `카탈로그`: `마켓 | 카테고리경로 | 코드 | 구분` — 각 오픈마켓에서 **선택 가능한 신발·의류 카테고리 전체**(현재 트리, 코드 포함). 비-패션/금지/부속잡화는 이미 걸러져 있음. `구분`은 매크로가 판정한 **신발 / 의류**.
  - `매핑`: `리프ID | 경로명 | 성별 | 구분 | 옥션 | G마켓 | 11번가 | 스마트스토어 | 쿠팡 | 롯데ON | LF몰 | 머스트잇 | 쇼피 | 큐텐JP | 플레이오토` — 각 리프(행)의 마켓 열은 비어 있음. `구분`은 그 리프가 신발인지 의류인지.
  - `설정`: `11번가카테고리종류`(국내/해외), `수집범위`(신발+의류/신발/의류).
- 출력: `매핑` 시트의 각 마켓 칸에 **그 마켓 `카탈로그`에 존재하는 카테고리경로 문자열을 그대로** 기입. (코드는 매크로가 카탈로그로 자동 해석하므로 경로만 정확히.)
  - **반드시 카탈로그에 있는 경로를 그대로** 쓸 것(오타/변형 금지 → 매크로가 코드를 못 찾음).
  - 빈 칸으로 두면 = **그 마켓은 기존값 유지**(덮어쓰지 않음). 적절한 카테고리가 없으면 **빈 칸**으로 둠(억지 매핑 금지).

## 매핑 규칙 (우선순위)
각 리프 × 각 마켓에 대해, 그 마켓 카탈로그 후보 중 아래를 만족하는 **가장 근접한 카테고리**를 고른다.

1. **구분 일치(필수·최우선)**: `구분=신발` 리프에는 `구분=신발` 카탈로그만, `구분=의류` 리프에는 `구분=의류` 카탈로그만 고른다. **신발↔의류 교차매핑 금지**(적용 시 매크로가 경고를 띄움).
2. **성별 일치(필수)**: 리프 성별(남성/여성)과 반대 성별 카테고리 배제. `공용/유니섹스`는 허용. (리프 성별이 '공용'이면 남/녀 무관.)
3. **11번가 = 매크로 패널에서 고른 종류(국내/해외)로 카탈로그가 이미 수집됨**: 11번가는 국내/해외가 **완전히 다른 카테고리 트리**이고, `카탈로그`의 11번가 후보는 패널 선택(ABC마트=**국내**)에 맞는 트리만 담겨 있다(매크로가 전용 엔드포인트로 그 종류만 수집). 따라서 11번가는 **카탈로그에 있는 후보 중 최근접 카테고리**를 고르면 된다.
   - 국내 신발 경로 형태 예: `남성신발 > 정장구두 > 정장구두`, `남성신발 > 로퍼 > 로퍼`, `남성신발 > 운동화 > 러닝화`, `여성신발 > 펌프스/힐 > 하이힐`, `등산/아웃도어 > 등산화 > 트레킹화`.
   - 국내 의류 경로 형태 예: `남성의류 > … > …`, `여성의류 > … > …`, `브랜드 남성의류 > …`, `브랜드 여성의류 > …`.
   - (참고: `패션잡화 > 남성화 …`나 `해외명품 …`은 11번가 **해외** 트리라 국내 카탈로그엔 안 나온다.)
   - ★ **11번가 = `디자이너` · `biz` 회피**: 경로에 `디자이너`(디자이너관) 또는 `biz`/`BIZ`/`비즈`(사업자 전용관)가 들어가면 **고르지 않는다**. 일반 판매용이 아니라 별도 입점·자격이 걸린 관이다. 11번가에서 `브랜드 남성신발`·`브랜드 여성의류`처럼 **`브랜드`로 시작하는 것은 정상 후보**다(브랜드 회피는 옥션·G마켓 전용 규칙 — 4번).
4. **옥션·G마켓 = 브랜드 회피**: 경로가 `브랜드…`로 시작/포함되면 피하고 비-브랜드 카테고리로. **11번가에는 적용하지 않는다**(위 3번).
5. **금지 영역 배제**: 어린이/유아/아동/키즈/주니어, 반려동물, 의료/위생, 낚시복·스키부츠·자전거화 같은 특수목적군은 배제(카탈로그에서 이미 걸러졌지만 재확인).
6. **부속/잡화 금지**: 신발용품(깔창·구두약·부츠키퍼), 양말·가방·모자·벨트 등 본품이 아닌 리프는 절대 고르지 않는다.

### 7-A. 신발 리프 → 마켓 카테고리 대응(예시)
- 로퍼/뮬블로퍼/모카신 → `로퍼`, `구두 > 로퍼`, `플랫슈즈/로퍼`(여성)
- 더비/레이스업/옥스퍼드/구두 → `구두`, `정장구두`, 국내 여성이 구두칸 없으면 `플랫슈즈`
- 부츠/워커/앵클/롱/레인/털/패딩부츠 → `부츠`, `부츠/워커`, `부츠/부티`(여성)
- 샌들/슬라이드/코르크/플립플랍/클로그/아쿠아 → `샌들`, `샌들/슬리퍼`, `조리/슬리퍼`, `쪼리/플립플롭`
- 슬리퍼/뮬 → `슬리퍼`, `샌들/슬리퍼`
- 힐/펌프스/웨지 → `펌프스/힐`, `하이힐`, `웨지힐`(웨지)
- 플랫/메리제인/발레 → `플랫슈즈`, `플랫`, `플랫슈즈/로퍼`
- 스니커즈/캔버스/단화/슬립온/라이프스타일 → `스니커즈`, `슬립온`, `캔버스화`, `스니커즈/운동화`
- 운동화/농구화/러닝/워킹/골프/등산/트레일 → `운동화`, `러닝화`, `워킹화`; 골프는 `골프화`, 등산은 `등산화`, 농구는 `농구화`가 있으면 그것

### 7-B. 의류 리프 → 마켓 카테고리 대응(예시)
- 긴팔티셔츠/반팔티셔츠/민소매 → `티셔츠`, `반팔티셔츠`/`긴팔티셔츠`, `민소매/나시`; 세분칸 없으면 `티셔츠`
- 후드/맨투맨 → `후드티/맨투맨`, `후드`, `맨투맨/스웨트셔츠`
- 니트/스웨터/카디건 → `니트/스웨터`, `카디건`; 카디건 칸 없으면 `니트/스웨터`
- 블라우스/셔츠 → `셔츠/남방`(남성), `블라우스/셔츠`(여성)
- 수트/정장 → `정장`, `수트`; 없으면 `자켓`류
- 자켓/점퍼 → `점퍼/자켓`, `재킷`, `블루종`
- 다운/패딩 → `패딩/다운`, `다운점퍼`; 없으면 `점퍼/자켓`
- 코트 → `코트`; 없으면 `아우터` 상위
- 베스트/조끼 → `베스트/조끼`; 없으면 `아우터` 상위
- 플리스 → `플리스/후리스`; 없으면 `점퍼/자켓`
- 긴바지/데님/반바지 → `팬츠/바지`, `청바지/데님`, `반바지/숏팬츠`
- 레깅스 → `레깅스`; 없으면 `트레이닝/운동복` 또는 `팬츠`
- 스커트 → `스커트/치마`
- 원피스 → `원피스`; 여성 전용 칸만 있으면 그것
- 셋업 → `셋업/세트`; 없으면 `정장` 또는 `아우터` 상위
- 언더/홈웨어 → `홈웨어/잠옷`, `언더웨어`; 성인 기준 칸만 사용
- 스포츠 피트니스 → `스포츠의류 > 피트니스/요가`; 없으면 `트레이닝복`

8. **"비슷하면 OK"**: 정확한 소분류 칸이 없으면 **같은 구분 안에서** 상위·근접 카테고리로. (예: 여성 '더비'는 11번가에 구두 칸이 없으니 `여성신발 > 플랫슈즈 > 플랫슈즈`; '남성 카디건'은 카디건 칸이 없으면 `남성의류 > 니트/스웨터`.)
9. **적합 후보가 전혀 없으면 빈 칸**(미매핑). 반대성별 / ESM 브랜드 / 11번가 디자이너·biz / 다른 구분밖에 없다면 채우지 말 것.

## 체크(자기검증)
매핑을 마치면 스스로 확인:
1. 모든 값이 **그 마켓 카탈로그에 실제로 존재**하는가(문자열 완전일치).
2. 리프 `구분`과 카탈로그 `구분`이 **모두 일치**하는가(신발↔의류 교차 0건).
3. 성별 위반 없는가.
4. 11번가가 국내 트리에서 골라졌는가 — 카탈로그가 이미 국내 트리만 담고 있으므로 카탈로그에 있는 후보를 그대로 쓰면 됨(이름만 보고 국내/해외를 재판단하지 말 것).
5. **11번가에 `디자이너` / `biz` / `비즈` 없는가.**
6. 옥션/G마켓에 '브랜드' 없는가(11번가는 해당 없음).
7. 부속/잡화(신발용품·양말·끈 등) 없는가.

## 진행 방식
- Claude는 카탈로그 후보가 많아도 **마켓별로 후보를 필터(구분→성별→유형)→최근접 1개**를 고른다. 전 리프 × 11마켓을 같은 규칙으로 일관 처리.
- 결과 엑셀(`매핑` 시트 채운 것)을 저장해 매크로 ③ 적용에 넣으면 그대로 저장된다.
- 적용 시 매크로가 **고시 상품군**을 리프 구분에 맞춰 자동 선택한다(의류 리프 → '의류' 고시군, 신발 리프 → '구두/신발' 고시군).
