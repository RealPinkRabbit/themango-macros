# 더망고 상품업데이트 런처 — 사용법

`더망고_상품업데이트_런처.user.js` · v1.9.1 (2026-08-08)

> ### ⚠ v1.9.0을 쓰고 있다면 v1.9.1로 갱신할 것
>
> v1.9.0은 진행 커서 3개만 되돌렸는데, 그것만으로는 부족했다. 2회차 로그가 `[1] [501] [2] [502] …`로 뒤섞여 **한 탭에서 두 작업이 도는 것처럼 보인다.** 원인과 대응은 3-3-4.
>
> (상품 처리 자체는 1갈래였다 — 작업 배열을 걷는 커서는 `inter_idx` 하나뿐이다. 번호만 두 계열로 갈린 것이다.)

> ## ★★ v1.9 — 반복 2회차가 늘 0건으로 끝나던 버그를 고쳤다
>
> **v1.5\~v1.8을 쓰고 있다면 반드시 갱신할 것. 그 버전들의 `반복`은 한 번도 제대로 동작한 적이 없다.**
>
> 증상은 이랬다 — 예약으로 건 1회차는 정상인데, 다음 날 반복 회차는 **마켓 로그인 체크만 하고 상품을 한 건도 처리하지 않은 채 "모두 완료되었습니다"로 끝났다.** 창 5개가 전부 같았다.
>
> 원인은 더망고의 **회차 커서 `inter_idx`**다. 회차 끝에 이 커서를 0으로 되돌리는 코드가 하필 `if(auto_repeat …)` 블록 안에 들어 있어서, **v1.4가 자동 반복을 끄면서 초기화까지 같이 꺼졌다.** 1회차가 끝나면 커서가 450(=대상 건수)에 멈춰 있고, 2회차는 대상 450건을 정상적으로 다시 실어 오고도 `450 < 450`이 거짓이라 곧장 완료로 빠진다. 자세한 것은 3-3-2.
>
> **v1.9가 두 가지를 한다.**
>
> - **시작 직전에 런처가 커서를 되돌린다**(`inter_idx`·`show_idx`·`send_count`). 반복 회차와 예약 시작 양쪽에 걸려 있다.
> - **회차가 끝나면 처리 건수를 배너에 적는다.** 0건이면 배너가 빨갛게 바뀌고 탭 제목이 `[대기·0건]` / `[완료·0건]`이 된다. 3-3-3 참고.
>
> ★ 이 버그가 하루를 통째로 날린 이유는 **실패가 조용했기 때문**이다. 초록 배너로 "N회차를 시작했습니다"가 뜨고 탭 제목도 `[진행중]`이 됐으며 완료 감지까지 정상이었다. **앞으로 반복이 잘 도는지 확인할 때는 종료 상태가 아니라 처리 건수를 볼 것.**

> **v1.7에 `실행+예약`이 생겼다.** 창을 지금 열어 두고, 프리셋의 **예약** 시각이 되면 각 창이 **대조를 다시 한 뒤** 스스로 시작한다. 마켓전송을 새벽에 돌리고 싶을 때 쓴다. 아래 2-6 참고.
>
> - 예약 시각이 이미 지났으면 **다음 날**로 잡는다.
> - 대기 중에 사람이 시작 버튼을 직접 누르면 **예약은 조용히 물러난다**(이중 시작 방지).
> - ★ **창을 닫거나 PC가 절전/종료되면 예약도 사라진다.** 대기는 창 안에서만 돈다.

> **v1.6부터 구간은 최대 5개다.** 동시창 한도가 사이트당 5창이므로(3-1) 구간도 그 이상 만들지 않는다.
> `1~4구간`은 지정한 크기(기본 450 / 500)대로 끊고, **5번째 구간이 남은 전량**을 맡는다 → 마지막 구간이 가장 커진다. 아래 2-3 참고.

> **v1.5부터 반복을 런처가 직접 한다.** 프리셋의 `반복`에서 `매일 HH:MM`을 고르면, 창이 1회차를 마친 뒤 그 시각에 **설정 대조를 다시 하고** 시작 버튼을 누른다. 더망고 자체 반복(`auto_repeat`)은 계속 꺼 둔다 — 그쪽은 루프를 겹쳐 늘리기 때문이다(3-3).
>
> - 다음 시각은 **직전 회차가 끝난 뒤에만** 계산한다 → 회차가 겹치거나 밀려 쌓이지 않는다.
> - 실행 중에 지정 시각을 넘겨 버리면 **그날은 건너뛰고 다음 날**로 간다.
> - 대기 중에도 실행 중에도 배너에 `반복 중지` 버튼이 있다. 대조에 실패하면 반복을 멈추고 사유를 남긴다.
> - 탭 제목으로 상태가 보인다 — `[진행중]` / `[대기]` / `[완료]` / `[반복중지]`.

> **v1.3.x 이하를 쓰고 있다면 반드시 갱신할 것.** 더망고의 자동 재시작(`auto_repeat`)이 **재시작 1회마다 작업 루프를 2\~3개로 늘리는** 구조라, 창을 방치하면 4시간짜리 구간이 12시간에도 끝나지 않고 진도가 뒤죽박죽이 된다. v1.4가 이 반복을 껐고, v1.5가 안전한 반복으로 대체했다. 원인 분석은 3-3.
>
> (v1.3.0 버그 — 자동시작이 항상 30초 대기 후 취소 — 은 v1.3.1에서 고쳤다. 원인은 3-8.)

매일 반복하는 **상품업데이트 & 마켓전송** 설정을 프리셋으로 저장해 두고, 상품 범위를 구간으로 나눠 **여러 창을 한 번에** 띄운다.

> 실행 버튼이 **셋**이다.
>
> | 버튼 | 하는 일 |
> |---|---|
> | `실행 (N창)` | 창만 연다. **시작은 사람이 누른다.** |
> | `실행+자동시작 (N창)` | 확인창 1회를 거친 뒤, 각 창이 설정 대조를 통과하면 **5초 뒤 스스로 시작한다.** |
> | `실행+예약 (N창)` | 확인창 1회를 거친 뒤, 각 창이 **예약 시각까지 기다렸다가** 대조를 다시 하고 시작한다. |
>
> 어느 쪽이든 **대조에 실패한 창은 시작하지 않는다.** 자세한 안전장치는 2-5·2-6 참고.

---

## 1. 설치

1. Tampermonkey → 새 스크립트 → `더망고_상품업데이트_런처.user.js` 내용 붙여넣기 → 저장
2. 크롬에서 이 사이트의 **팝업 허용**을 켠다 (주소창 오른쪽 아이콘 → 항상 허용). 안 켜면 첫 창 하나만 열리고 멈춘다.
3. 더망고 **로그인 + 크롬 확장 설치** 상태여야 업데이트가 실제로 돈다. 확장이 없으면 시작을 눌러도 진행되지 않는다.

설치 후 `상품업데이트 & 마켓전송` 화면 오른쪽 위에 **상품업데이트 런처** 패널이 뜬다.

---

## 2. 기본 흐름

```
프리셋 만들기 → 건수 조회 → 구간 확인 → 실행 ─┬─ [실행]         열린 창마다 시작 버튼 클릭
                                             ├─ [실행+자동시작] 확인창 1회 → 창마다 5초 뒤 자동 시작
                                             └─ [실행+예약]     확인창 1회 → 창마다 예약 시각까지 대기 후 시작
                                                              ↓
                                                     끝나면 창을 닫는다 (중요)
```

### 2-1. 프리셋 만들기

| 항목 | 설명 |
|---|---|
| 이름 | 구분용. 예 `국내자라-옥션G마켓` |
| 수집사이트 | **하나만** 고른다. 창 한도가 사이트별로 걸리므로 프리셋도 사이트별로 나눈다 |
| 업데이트 항목 | 원문(수집사이트) → 더망고 갱신. 기본값 **가격 + 재고**. 비우면 업데이트를 건너뛰고 전송만 |
| 전송 마켓 | 더망고 → 마켓 전송. 범위 분할을 쓰면 이 값이 **구간 기본값**이 된다 |
| **검색조건** | ★v1.10 신설. 화면 검색폼의 상세 조건(검색유형+검색어·마켓등록여부·날짜검색·상품상태·통화·가격/상품번호 범위)을 통째로 담는다. 아래 2-1-1 참고 |
| 변동일 | 최근 N일 안에 가격·재고가 바뀐 상품만. 아래 3-5 참고 |
| 예약 | `예약 안 함` 또는 `지정 시각에 시작`. **`실행+예약` 버튼이 쓰는 값**이다. 아래 2-6 참고 |
| 반복 | `반복 안 함` 또는 `매일 HH:MM`. 창이 열려 있는 동안 런처가 그 시각에 다시 시작한다. 아래 3-3-1 참고 |
| 정렬 | **상품수집 날짜순(과거순) 고정.** 아래 3-2 참고 |
| 범위 분할 | 총건수·첫구간·이후구간 → 구간 목록 자동 생성 |

프리셋을 고르지 않고 그냥 쓸 수 있다. 드롭다운 기본값이 `-- 선택 안함 --`이고, 이 상태에서도 설정을 채워 바로 `실행`하면 된다. 다시 쓸 설정만 `저장`으로 남기면 된다(브라우저 localStorage).

> **상품상태**는 기본값이 `판매상품(재고+품절)`이다. v1.10부터는 **검색조건에 담기면 담긴 값이 이긴다** — 패널에 따로 칸을 두지 않고 화면에서 골라 담는다.

**업데이트 항목 — `모든정보`는 배타적이다.** `모든정보`를 켜면 나머지 4개가 꺼지고, 나머지 중 하나를 켜면 `모든정보`가 꺼진다. 화면 동작과 같다.

> 강의 권고: 가격·재고만 업데이트하면 **현재 수집된 옵션에 대해서만** 갱신되고 새 옵션은 안 들어온다. 속도는 항목별로 동일하므로 특별한 경우가 아니면 `모든정보`를 쓰는 편이 안전하다.

### 2-1-1. ★ 검색조건 담기 (v1.10)

**왜 생겼나** — 같은 수집사이트를 두 사업자가 나눠 쓴다. ABC마트 상품이 그렇다.
`수집사이트 = ABCmart` 만으로는 두 사업자 물건이 섞여 한꺼번에 나간다.

2026-09-04 실측:

| 조건 | ABC마트 상품 수 |
|---|---|
| 조건 없음(전체) | 7,948 |
| 검색유형 `검색필터명 검색` + 검색어 `솔데글GL` | **4,904** |
| 검색유형 `검색필터명 검색` + 검색어 `솔데글KR` | **3,044** |

합이 정확히 전체와 같다 — **검색필터명으로 두 사업자가 깔끔하게 갈린다.**
(적용정책명 검색은 `솔데글GL`이 0건이라 못 쓴다. ABC마트 GL 상품에는 다른 이름의 정책이 붙어 있다.)

**쓰는 법**

1. 페이지 위쪽 **검색폼에서 조건을 고른다**(검색을 누를 필요는 없다 — 폼 상태를 그대로 읽는다).
2. 패널의 **`현재 화면 조건 담기`** 를 누른다 → 프리셋에 담기고 요약이 뜬다.
3. **`건수 조회`를 다시 누른다.** 조건이 바뀌면 이전 건수·구간은 더 이상 맞지 않으므로 런처가 총건수를 0으로 되돌린다.
4. `저장`하면 프리셋에 남는다. 사업자별로 프리셋을 하나씩 만들어 두면 끝이다.

`비우기`를 누르면 조건이 없어지고 화면 기본 조건(=좁히지 않음)으로 돌아간다.
조건을 담지 않은 상태에서는 패널 요약이 **빨간 글씨 `담긴 조건 없음`** 으로 뜬다.

**담기에서 빠지는 것** — 런처가 이미 자기 항목으로 갖고 있는 것들이다. 소유자를 하나로 두어 "어느 쪽이 이기는가"가 생기지 않게 했다.

`수집사이트` · `전송마켓` · `업데이트항목` · `변동일` · `정렬`(uid_asc 고정) · 페이지 번호

**담기는 것** (2026-09-04 실측, 전부 URL로 화면에 복원되는 것까지 확인)

| 화면 항목 | 파라미터 | 비고 |
|---|---|---|
| 검색유형 9종 + 검색어 | `search_type` + `ps_subject` | 상품명 / 상품번호 / 수집사이트상품번호 / 전송마켓상품번호 / 가격(원가) / **검색필터명** / 적용정책명 / 상품메모 / 브랜드명(원문) |
| 상품번호 검색방식 | `goods_id_mode` | 일반검색 / 범위검색 |
| 마켓 등록·미등록 46종 | `ps_market_id` | 전체마켓 미등록(신규) · 마켓별 등록/미등록 · ★**마켓×사업자계정별**(`옥션2.0-soldeglegl` 등)도 있다 |
| 날짜검색 **체크박스** | `ps_duse=1` | ★★ **이걸 켜지 않으면 서버가 기간을 통째로 무시한다.** 꺼진 채로 담으면 날짜가 안 걸린다 |
| 날짜 종류 | `date_type` | 빈값=등록일자 / `1`=업데이트일자 / `2`=가격·재고변동일자 |
| 기간 | `start_yy/mm/dd`, `end_yy/mm/dd` | |
| 상품상태 | `ps_status` | 전체 / 판매(기본) / 재고 / 품절 / 삭제 |
| 통화 | `ps_currency` | |
| 원가 범위 | `ps_price_min` / `ps_price_max` | |
| 상품번호 범위 | `ps_gid_min` / `ps_gid_max` | |

- 런처가 **모르는 파라미터는 요약에 `이름=값` 원문 그대로** 표시된다. 화면에 안 보이는 조건이 조용히 걸리는 일은 없다.
- 더망고가 검색항목을 새로 추가해도 **코드를 고칠 필요가 없다.** 폼을 통째로 읽기 때문이다.

**조건은 전 구간에 똑같이 걸린다.** `건수 조회` → 구간 분할 → 각 창의 URL → 예약·반복 회차까지 전부 같은 조건을 쓴다.

**★ 대조에도 들어간다.** 새 창은 담긴 조건이 화면에 그대로 복원됐는지 확인하고, 하나라도 다르면 **시작 버튼을 잠그고 빨간 배너**로 어느 항목이 어긋났는지 보여 준다(`조건 검색어 — 요청 [솔데글KR] / 실제 [솔데글GL]` 형태). 조건이 조용히 빠진 채로 도는 것이 이 기능에서 가장 위험한 실패이기 때문이다 — 엉뚱한 사업자 상품이 마켓으로 나간다.

### 2-2. 건수 조회

`건수 조회` 버튼은 **현재 화면이 아니라 프리셋 조건 그대로** 서버에 조회해 건수를 가져온다.
화면 조건과 프리셋이 달라도 틀린 구간이 만들어지지 않는다.

상품이 늘면 이 버튼만 다시 누르면 구간이 재계산된다.

### 2-3. 구간

기본 **첫 구간 450건, 이후 500건씩**. 두 값 모두 바꿀 수 있다.

**★ 구간은 최대 5개다 (v1.6).** 동시창 한도가 사이트당 5창이므로 그 이상 만들지 않는다.
`1~4구간`을 지정한 크기로 끊고 나면 **5번째 구간이 나머지 전부**를 맡는다.

```
9,159건 → 5구간
1 ~ 450 / 451 ~ 950 / 951 ~ 1450 / 1451 ~ 1950 / 1951 ~ 9159
                                                 └ 마지막 구간이 7,209건
```

> **마지막 창이 가장 오래 걸린다.** 창 수 상한을 지키기 위한 의도된 동작이다.
> 앞 4구간이 먼저 끝나 창이 놀게 되므로, 앞쪽을 크게 잡아 균형을 맞추고 싶으면 `첫`·`이후` 값을 올리면 된다
> (예: 9,159건을 다섯으로 고르게 나누려면 둘 다 `1832` → `1~1832 / … / 7329~9159`).

총건수가 작아 4구간 안에 끝나면 5번째 구간은 생기지 않는다 (예: 1,200건 → `1~450 / 451~950 / 951~1200` 3구간).

구간마다 체크박스가 있다. **체크 해제하면 그 구간은 열지 않는다** — 실패한 구간만 다시 돌리거나, 앞쪽 몇 개만 먼저 돌릴 때 쓴다. 목록 위의 `전체선택` / `전체해제`로 한 번에 처리할 수도 있다.

체크박스를 연속으로 눌러도 목록이 다시 그려지지 않으므로 **스크롤 위치가 그대로 유지된다.**

### 2-3-1. 구간별 전송 마켓

구간 줄 오른쪽의 `[옥션2.0, G마켓2.0 ▾]`을 누르면 **그 구간만의 마켓**을 고를 수 있다.

```
[x] 1. 1~450     [옥션2.0, G마켓2.0 ▾]
[x] 2. 451~950   [옥션2.0 ▾]          ← 이 구간만 옥션
[x] 3. 951~1450  [옥션2.0, G마켓2.0 ▾]
```

- 손대지 않은 구간은 위쪽 **전송 마켓(구간 기본값)** 을 따른다.
- 따로 지정한 구간은 링크가 **빨간색**으로 표시된다.
- `이 마켓을 전 구간에 적용`을 누르면 그 구간의 마켓이 기본값이 되고 개별 지정은 모두 해제된다.

### 2-4. 실행

`실행 (N창)` → 선택한 구간을 **전부** 1.2초 간격으로 연다. 창 수 제한이나 확인창은 없다.

팝업이 차단되면 그 지점에서 멈추고 `남은 N개 열기` 버튼이 나타난다. 팝업을 허용한 뒤 그 버튼을 누르면 이어서 연다.

> 강의 기준 권장 창 수(업데이트 전용 5창 / 전송 포함 1\~3창)는 **열린 창의 배너**에 안내로만 표시된다. 막지 않는다.

### 2-5. 실행+자동시작

`실행+자동시작 (N창)`을 누르면 **확인창이 한 번** 뜬다. 무엇이 나가는지 한 화면에 모아 보여 준다.

```
[국내자라-옥션G마켓] 자동 시작

· 수집사이트   : Zara.com/kr (국내자라)
· 업데이트항목 : 모든정보
· 전송마켓     : 옥션2.0, G마켓2.0
· 변동일       : 전체
· 정렬         : 상품수집 날짜순(과거순)
· 창 5개 · 대상 2,450건 (1 ~ 2,450)

각 창은 설정 대조를 통과하면 5초 뒤 스스로 시작합니다.
(창마다 취소 버튼이 뜹니다. 대조에 실패한 창은 시작하지 않습니다.)

마켓에 전송된 내용은 되돌리기 어렵습니다. 진행할까요?
```

- 전송마켓을 구간별로 다르게 지정했으면 **`★ 구간별로 다름`** 으로 표시된다.
- 업데이트 항목도 전송 마켓도 없으면 확인창을 띄우지 않고 거부한다.
- 승인 후에는 창마다 다시 묻지 않는다. 대신 각 창에서 **5초 카운트다운 + `자동 시작 취소` 버튼**이 뜬다.

**창 하나가 시작하기까지 통과해야 하는 관문**

| # | 검사 | 실패하면 |
|---|---|---|
| 1 | 프리셋 ↔ 화면 대조 (사이트·항목·마켓·정렬·상태) | 시작 버튼 잠금 + 자동시작 중단 |
| 2 | 범위 값이 입력칸에 그대로 들어갔는가 | 중단 |
| 3 | 더망고 페이지 로딩 완료 (보통 1초 이내, 최대 60초 대기) | 중단 |
| 4 | 5초 카운트다운 (`자동 시작 취소` 버튼으로 취소 가능) | 중단 |
| 5 | **클릭 직전 재대조** — 대기 중 화면이 바뀌었을 수 있다 | 중단 |
| 6 | **범위 입력 영역이 열려 있는가** (아래 3-6) | 중단 |
| 7 | 클릭 후 버튼 라벨이 `…진행중입니다…` 로 바뀌는가 | 아래 판정표대로 |

**7번 — 시작을 눌러 본 뒤의 판정은 더망고의 응답으로 한다** (v1.3.1)

| 더망고 반응 | 런처 |
|---|---|
| 버튼 라벨이 `…진행중입니다…` 로 바뀜 | ✅ 시작 성공 |
| `페이지 로딩중입니다` alert | 1.5초 뒤 **재시도** (최대 60초) |
| 그 밖의 alert (사이트 미선택, 범위 오류 등) | ⛔ 중단 + **그 문구를 배너에 그대로** 표시 |
| alert도 없고 시작도 안 됨 | ⛔ 중단 (버튼이 죽은 상태 — 조용히 반복하지 않는다) |

중단된 창은 **빨간 배너에 사유가 남는다.** 확인 후 직접 시작하면 된다.

> **취소는 `자동 시작 취소` 버튼으로만 된다.** 배너 아무 곳이나 눌러도 취소되지 않는다.
> 버튼은 처음 한 번만 그려지고 카운트다운 중에도 다시 그려지지 않으므로, 누른 클릭이 유실되지 않는다.
> 사용자가 눌러서 멈춘 경우에는 배너에 **`[자동 시작 취소] 버튼을 눌렀습니다`** 라고 명시된다 — 다른 사유와 구별된다.

### 2-6. ★ 실행+예약 (v1.7)

> ## ⛔ 예약·반복을 쓸 때 화면을 끄지 마세요
>
> **이 PC는 Modern Standby 기기라, 화면을 끄는 순간 Windows가 크롬을 통째로 재웁니다.**
> 예약 대기도, 실행 중인 작업도 함께 멈춥니다. 2026-08-05 실측에서 화면을 끈 10분 동안 **진도가 1건도 나가지 않았고**, 예약은 지정 시각이 아니라 **화면을 켠 시점**에 시작됐습니다.
>
> **자면서 돌리려면 화면을 끄지 말고 밝기만 최저로 낮추세요.** `유틸리티/화면끄기.bat`를 쓰면 안 됩니다.
> 절전 설정(`powercfg`)이나 크롬 플래그로는 막을 수 없습니다 — OS 전원 상태이기 때문입니다. 자세한 내용은 `CLAUDE.md` 6-1장.

**창을 지금 열어 두고, 지정한 시각이 되면 시작한다.** 마켓전송을 새벽에 돌리고 싶은데 그 시간에 컴퓨터 앞에 없을 때 쓴다.

**쓰는 법**

1. 프리셋의 **예약**을 `지정 시각에 시작`으로 바꾸고 시·분을 고른다 (기본 `01:00`)
2. `실행+예약 (N창)` 클릭 → 확인창에서 **언제 시작하는지** 확인
3. 창이 열리고 각 창에 **⏰ 노란 배너 + 남은 시간 카운트다운**이 뜬다 (탭 제목 `[예약]`)
4. 그 시각이 되면 각 창이 **대조를 다시 하고** 시작한다 (탭 제목 `[진행중]`)

```
[국내자라-옥션G마켓] 예약 실행

· 수집사이트   : Zara.com/kr (국내자라)
· 업데이트항목 : 없음 (전송만)
· 전송마켓     : 옥션2.0, G마켓2.0
· 변동일       : 최근 1일
· 정렬         : 상품수집 날짜순(과거순)
· 시작         : ★ 내일 01:00 (약 10시간 30분 뒤) — 그때까지 창을 열어 둬야 합니다
· 반복         : 없음 (1회만)
· 창 5개 · 대상 9,159건 (1 ~ 9,159)

창을 지금 열어 두고, 내일 01:00에 각 창이 대조를 다시 한 뒤 시작합니다.
(창마다 [예약 취소] 버튼이 뜹니다. 대조에 실패한 창은 시작하지 않습니다.)
★ 창을 닫거나 PC가 절전/종료되면 예약도 함께 사라집니다.

마켓에 전송된 내용은 되돌리기 어렵습니다. 진행할까요?
```

**알아둘 것**

| | |
|---|---|
| 시각이 이미 지났으면 | **다음 날**로 잡는다. `14:30`에 `01:00`을 예약하면 내일 01:00 |
| 대기 중에 시작 버튼을 직접 누르면 | **예약이 조용히 물러난다.** 이중 시작이 되지 않는다 → *"지금 바로 돌리고 싶다"* 면 그냥 시작 버튼을 누르면 된다 |
| `예약 취소` 버튼 | 예약만 무른다. 창은 그대로 남고 시작은 사람이 누른다 |
| 시작 직전 대조에 실패하면 | 시작하지 않고 **빨간 배너에 사유**를 남긴다 (탭 제목 `[예약취소]`) |
| 예약으로 시작한 뒤 반복은 | **`반복` 설정을 그대로 따른다.** 반복이 꺼져 있으면 1회만 돌고 `🏁 완료` |
| 창을 닫으면 | 예약도 사라진다. **PC 절전·최대절전·화면 끄기도 마찬가지** — 위 경고 참조 |
| 예정 시각을 **30분 넘겨** 깨어나면 (v1.8) | **시작하지 않는다.** 빨간 배너에 얼마나 늦었는지와 사유를 남긴다 → 아침에 "왜 안 돌았나"를 바로 알 수 있다 |
| 30분 이내로 늦으면 (v1.8) | 시작하되 초록 배너에 `(예정보다 N분 늦음)`을 표기한다 |

> **★ 대기 중 `⚠ 최근 틱 N초` 경고가 보이면 (v1.8)** — 크롬이나 Windows가 이 창을 억제하고 있다는 뜻입니다. 정상이면 1초입니다. 이 경고가 뜬 채로 두면 예약이 제시간에 시작하지 않습니다. **화면을 켜고 밝기만 낮추세요.** 창을 최소화했거나 다른 창으로 완전히 덮은 경우에도 뜹니다.

> **정확도는 분 단위로 보면 된다.** 크롬은 화면에 보이지 않는 창의 타이머를 늦추므로(백그라운드 스로틀링) 실제 시작이 **최대 몇 분 늦을 수 있다.** 지정 시각보다 일찍 시작하는 일은 없다.

> **예약과 반복을 같이 켜면** — 예약 시각에 1회차를 시작하고, 끝난 뒤부터는 반복 시각을 따른다. 둘을 같은 시각으로 두는 것이 보통이다(예: 예약 `01:00` + 반복 `01:00` → 매일 새벽 1시).

> **`실행+자동시작`과 언제 갈리나** — 지금 바로 돌릴 거면 자동시작, 나중에 돌릴 거면 예약이다. 안전장치(대조·범위 확인·시작 판정)는 양쪽이 같다.

---

## 3. 알아둘 것

### 3-1. 창을 몇 개 띄울지는 조합에 따라 다르다 (강의 3-24, 3-35)

런처는 막지 않는다. 아래는 판단 기준이다.

| 프리셋 조합 | 권장 | 왜 |
|---|---|---|
| 업데이트만 (마켓 미선택) | 사이트당 **5** | 리소스를 나누지 않아 **창 수만큼 빨라진다** |
| 전송만 (항목 미선택) | **1** 권장 | *"3개 띄운다고 3배속이 안 돼요. 리소스를 3개로 나눠요"* — 나눠도 효과가 거의 없다 |
| 스마트스토어 단독 전송 | **3** | 과속하면 덮어쓰기 오류가 난다 |
| 업데이트 + 전송 | 3 | 전송 쪽이 병목 |

**구간을 잘게 나눠 이득을 보는 건 "업데이트만" 프리셋이다.** 전송이 섞인 프리셋을 10개로 쪼개도 빨라지지 않는다.

### 3-2. 정렬은 과거순 고정

`상품수집 날짜순(과거순)` = uid 기준이라 회차마다 순서가 흔들리지 않는다.
날짜순(상품업데이트 날짜순 등)으로 정렬하면 **오늘의 1\~450번과 내일의 1\~450번이 다른 상품**이 되어 구간이 의미를 잃는다. 그래서 선택지로 열어 두지 않았다.

v1.10에서 검색조건을 담을 수 있게 한 뒤에도 **정렬만은 담기에서 제외**해 고정을 유지했다. 같은 이유다.

### 3-3. ★★ 더망고 자동 재시작(`auto_repeat`)은 루프를 **겹쳐 늘린다** — v1.4가 끄고, v1.5가 대체한다

**이 반복은 더망고 기능이지 Tampermonkey나 이 런처와는 무관하다.** 다만 결과가 예상보다 훨씬 나쁘다.

`admin_goods_update.php`를 열면 더망고 서버가 페이지 안에 `auto_repeat = "Y"`를 직접 심어 보낸다(계정 설정값). **2026-08-02에 소스를 직접 뜯어 보니** `scrapDetailParallel()`의 끝이 이렇게 생겼다.

```js
if (auto_repeat) {
    setTimeout(function(){ … scrapDetailParallel(); }, seconds);   // ①
    setTimeout(function(){ auto_restart_ini();      }, seconds);   // ②
} else { … }
```

그리고 `auto_restart_ini()`는 두 줄이다 — `update_page_load('1'); scrapDetailParallel();`
게다가 `update_page_load()`의 ajax 콜백도 **끝에서 무조건** `scrapDetailParallel()`을 부른다(조건 없음).

> **재시작 1회 = 작업 루프가 2\~3개로 늘어난다.**
> ②만으로 이미 2개다(②가 직접 1개 · ②가 부른 `update_page_load` 콜백에서 1개). ①까지 걸리면 3개.
> `scrapDetailParallel()`에는 **재진입 가드가 없다.** 늘어난 채로 또 완료되면 또 겹친다.
>
> ⚠️ **런타임 실측에서는 ②만 걸리고 ①은 안 걸렸다.** 소스상 나란히 있지만 ①에는 별도 조건(스크래퍼 팝업 상태로 추정)이 붙는 것으로 보인다 — 미규명. 그래서 배수는 상황에 따라 2\~3배로 본다.

**★ 재시작 시각 — 랜덤의 기준점은 "완료 시점"이 아니라 "지정 시각"이다.**

`auto_repeat_time="01"`(=01시)이면 **다음 01시\~02시 사이 랜덤**에 재시작한다(더망고 설명과 동일). 실측: 18:46:38에 끝난 창에 걸린 지연이 **25,401,022ms = 7시간 3분 21초** → 재시작 예정 **01:49:59** = `01:00 + 49분 59초`.

랜덤 폭(0\~60분)은 맞지만 **기준이 완료 시점이 아니라 01시**다. 그래서 저녁에 끝난 창이 7시간을 기다린다.

**★ 랜덤은 창마다 따로 뽑힌다.** 창 5개면 01:0x·01:2x·01:5x… 제각기 재시작한다 → 창별 진도가 어긋나 보이는 원인 중 하나다.

**★ 반복이 켜져 있으면 완료해도 버튼이 `진행중`으로 남는다.** `update_btn_change('on')`이 `else` 쪽에만 있어서다. 재시작을 기다리는 창이 **일하는 중처럼 보인다.**

실제 사고(2026-08-02 보고): 4시간이면 끝날 구간이 **12시간 뒤에도 안 끝났고**, 창마다 진도가 들쑥날쑥했다. 새벽 01시대에 각 창이 제각기 재시작하면서 루프가 겹친 결과다.

**v1.4부터 런처가 연 창은 이 반복을 끈다.** 자동시작이든 수동시작이든 똑같이 끈다. **반복이 필요하면 v1.5의 런처 반복(3-3-1)을 쓴다.**

> **"`auto_restart_ini`만 죽이고 `auto_repeat`은 살린다"는 안은 기각됐다**(2026-08-02 실측). 완료 시점에 실제로 걸린 타이머가 ② 하나뿐이라, `auto_restart_ini`를 죽이면 **재시작이 아예 사라진다.** 게다가 버튼은 `진행중`으로 굳고 창은 7시간을 아무것도 안 하며 기다린다 — 반복이 살아 있는 것도, 깔끔히 꺼진 것도 아닌 최악의 상태다.

> ★ **끄는 방법에 함정이 있다.** 조건이 `auto_repeat == 'Y'` 비교가 아니라 **`if(auto_repeat)` 진리값**이다.
> `"N"`도 truthy라 **값을 `"N"`으로 바꿔서는 안 꺼진다.** 매크로는 빈 문자열(`''`)로 만든다.
> 더망고 **환경설정**에서 자동반복을 끄는 것도, 서버가 `"N"`을 심어 보내는 방식이라면 효과가 없을 수 있다(미확인).

차단에 성공하면 배너에 `🔒 더망고 자동 재시작(auto_repeat)을 껐습니다`가 뜬다. 실패하면 대신 빨간 경고가 뜨고, 그때는 **끝난 창을 반드시 닫아야 한다.**

배치가 끝나면 배너가 `🏁 이 구간이 끝났습니다`로 바뀌고 탭 제목 앞에 `[완료]`가 붙는다. 창을 닫아도 되고 그냥 둬도 아무 일도 일어나지 않는다.

### 3-3-1. ★ 런처 반복 — 프리셋의 `반복` (v1.5)

반복이 필요하면 더망고 기능 대신 **런처가 직접 재시작한다.** 프리셋에서 고른다.

```
반복  [반복 안 함 / 매일 ▾]  [00~23시 ▾]  [00~59분 ▾]
```

`매일`로 바꾸면 기본값 `01:00`으로 열린다. 저장값은 `'H:M'` 문자열 하나다(v1.5.0이 저장한 시-only 값도 그대로 읽히며 00분으로 본다).

**동작 순서**

1. 1회차가 끝난 것을 감지한다
2. `🔁 N회차 완료. 매일 HH:MM에 다시 시작합니다` 배너 + **남은 시간 카운트다운** (탭 제목 `[대기]`)
3. 지정 시각이 되면 **프리셋↔화면 대조를 다시 하고**, 범위 값·범위 UI 열림까지 재확인
4. 통과하면 시작 버튼 클릭 → `▶ N회차를 시작했습니다` (탭 제목 `[진행중]`)
5. 완료되면 2번으로

**왜 더망고 것보다 안전한가**

| | 더망고 `auto_repeat` | 런처 반복 (v1.5) |
|---|---|---|
| 재시작 시 루프 수 | **2\~3개** (겹침) | **1개** (평소 시작 경로 그대로) |
| 회차마다 설정 대조 | 안 함 | **함** — 어긋나면 멈추고 사유를 남김 |
| 시각 | 서버 설정값, 화면에 안 보임 | 프리셋에서 지정, 남은 시간이 배너에 보임 |
| 중지 | 창을 닫는 수밖에 없음 | `반복 중지` 버튼 (대기 중·실행 중 모두) |

**★ 다음 시각은 직전 회차가 끝난 뒤에만 계산한다.** 실행 중에는 타이머가 아예 돌지 않으므로 회차가 겹치거나 밀려 쌓이는 일이 구조적으로 없다. 대신 **실행 중에 지정 시각을 넘겨 버리면 그날은 건너뛰고 다음 날로 간다.**

> ⚠️ 창 5개 중 느린 구간 하나만 시각을 넘기면 **그 창만 하루를 건너뛰어** 다른 창들과 회차가 어긋난다. 구간별 소요 시간이 지정 시각을 사이에 두고 갈릴 만하면 여유 있는 시각을 고를 것.

잘못된 값(빈 값·범위 밖)은 **반복하지 않는 것으로 처리한다.** 조용히 00시로 떨어지지 않게 했다.

### 3-3-2. ★★ v1.9가 고친 것 — 회차 커서 (2026-08-07 실측)

**v1.5\~v1.8의 반복은 2회차부터 늘 0건이었다.** 예약으로 건 1회차만 정상이었다.

관측된 로그는 이랬다.

```
선택하신 마켓으로 상품전송 하기 전에 로그인을 체크합니다.
......[마켓로그인체크] [쿠팡] … → 로그인에 성공하였습니다.      (10건 전부 성공)
검색조건에 맞는 상품의 재고, 가격 업데이트 및 선택하신 마켓으로 상품 전송을 시작합니다.
......상품의 가격 업데이트 및 선택하신 마켓으로 상품 전송이 모두 완료되었습니다.
```

시작합니다 다음에 **상품별 처리 줄이 한 줄도 없다.** 대상이 0건이었다는 뜻이다.

**목록 재적재는 문제가 아니었다.** `start_ini_all()`은 끝에서 `update_page_load('all')`을 부르고, 그 안에서 `#all_update_market`에 shell을 다시 실어 `a_checked`(작업 배열)를 새로 채운다. `#qry`·`#order_sql`은 그 컨테이너 바깥이라 살아남으므로 검색조건도 온전하다. 처음 세운 "목록이 소진돼서"라는 가설은 **틀렸다.**

**진짜 원인은 전역 커서 `inter_idx`다.**

```js
function scrapDetailParallel(){
    if (inter_idx < a_checked.length) {        // ★ 일할지 말지 정하는 유일한 관문
        ... a_checked[inter_idx] 처리 ...
    } else {
        ... "모두 완료되었습니다" 로그 ...
        if (auto_repeat && start_mode == '…') {          // ← else 절의 거의 전부
            inter_idx = 0;  show_idx = 0;  send_count = 0;
            st11_idx = 1;  auction20_idx = 1;  ...        // 마켓 순번 34개
            ... 재시작 타이머 ...
        }
    }
}
```

**커서 초기화 37개가 예외 없이 `if(auto_repeat …)` 블록 안에 있다.** 중괄호 매칭으로 확인했다 — 함수 전체 712토큰 중 그 블록이 247\~710이다.

즉 `auto_repeat`은 **재시작 타이머만이 아니라 회차 간 상태 초기화까지 겸하고 있었다.** v1.4가 이 플래그를 끄면서 초기화도 같이 꺼졌고, 1회차가 끝나면 `inter_idx`가 450(=대상 건수)에 멈춘 채 남았다. 2회차는 450건을 정상적으로 다시 실어 오고도 `450 < 450`이 거짓이라 곧장 완료로 빠진 것이다.

**대응** — `verifyAndStart()`가 시작 버튼을 누르기 직전에 커서를 되돌린다. 반복 회차와 예약 시작이 같은 함수를 쓰므로 양쪽에 함께 걸린다. **v1.9.1부터 더망고와 똑같이 37개를 전부 되돌린다**(v1.9.0은 3개만 되돌렸다가 3-3-4의 증상을 냈다).

- **진행 커서 3개**(`inter_idx`·`show_idx`·`send_count`) — 전역을 **찾지 못하면 시작하지 않는다.** 그냥 대입하면 페이지가 쓰지 않는 전역을 새로 만들 뿐이고 진짜 커서는 그대로라 또 조용히 0건으로 끝나기 때문이다.
- **마켓 순번 34개** — 없으면 조용히 건너뛴다. ★ 더망고의 초기화 목록에는 `test0_idx`·`test1_idx`가 들어 있는데 **이 페이지에는 그 전역이 없다**(실제로 있는 것은 `test_idx`다). 없는 것을 이유로 시작을 막으면 런처가 아예 못 돈다.

### 3-3-4. ★ 로그가 `[1] [501] [2] [502]`로 뒤섞이던 것 (v1.9.1에서 해소)

v1.9.0에서 2회차 로그가 두 계열로 갈려, 한 탭에서 두 작업이 동시에 도는 것처럼 보였다.

마켓 전송 함수는 전부 이렇게 시작한다.

```js
function api_coupang(uid){
    if (coupang_idx <= show_idx)  coupang_idx = eval(show_idx);   // ★ 한 방향으로만 맞춘다
    ...
    layer_log_append('…' + coupang_idx + '…');                    // 로그 줄 번호 = 이 값
    coupang_idx++;
}
```

`api_11st` · `api_auction20` · `api_gmarket20` · `api_smartstore` 전부 같다(5개 마켓 실측 확인).

★ **자가보정이 한 방향뿐이다.** 마켓 순번이 `show_idx`보다 뒤처지면 따라 올라오지만, **앞서 있으면 절대 내려오지 않는다.** 그래서 v1.9.0처럼 `show_idx`만 0으로 되돌리면 이렇게 된다.

```
1회차 종료 : show_idx = 500,  coupang_idx = 501
v1.9.0     : show_idx = 0     coupang_idx = 501   ← 마켓 순번은 안 건드렸다
2회차 시작 : 501 <= 0 이 거짓 → 자가보정 안 걸림

  업데이트 로그 → show_idx 계열   → [1], [2], [3] …
  쿠팡 전송 로그 → coupang_idx 계열 → [501], [502] …
```

**작업이 두 갈래로 갈린 것은 아니다.** 작업 배열 `a_checked`를 걷는 커서는 `inter_idx` 하나뿐이고, 마켓 순번은 로그 줄 번호와 서버 요청의 `show_idx` 파라미터 값으로만 쓰인다. 확인하려면 **회차 완료 배너의 `처리 N건 / 대상 M건`**을 볼 것 — 두 배로 돌았다면 여기서 어긋난다.

> ★ 교훈: **반쯤만 되돌린 상태가 아무것도 안 되돌린 것보다 나쁠 수 있다.** "검증하지 않은 변수는 건드리지 않는다"는 보통 옳지만, 서로 동기화되는 변수 묶음에서는 일부만 손대면 그 동기화가 깨진다. 자가보정이 있다는 것을 봤으면 **어느 방향으로만 도는지**까지 확인할 것.

### 3-3-3. ★ 0건 경고 (v1.9)

회차가 끝나면 런처가 `inter_idx`를 읽는다. 시작 직전에 0으로 되돌렸으므로 **그 값이 곧 이번 회차의 처리 건수**다.

| 배너 | 탭 제목 | 뜻 |
|---|---|---|
| 🟦 `🔁 … 2회차 완료 (처리 450건 / 대상 450건)` | `[대기]` | 정상 |
| 🟥 `🔁 … 2회차 완료 (처리 0건 / 대상 450건)` + `⚠ 이 회차는 한 건도 처리하지 않았습니다` | `[대기·0건]` | 아래 참조 |

**0건이어도 반복은 멈추지 않는다.** 정당하게 0건이 되는 경우가 있기 때문이다 — 총건수가 줄어 **구간이 검색결과 밖으로 밀려난 경우**다(예: 5번 구간 `1951~3500`인데 총건수가 1,800으로 줄면 그 구간은 정상적으로 0건이다). 여기서 반복을 죽이면 오탐 한 번에 창이 죽는다.

그러니 **0건 경고를 보면 먼저 구간이 아직 유효한지부터 확인할 것.** 구간이 멀쩡한데 0건이면 커서가 되돌려지지 않은 것이다.

### 3-4. 열린 창의 배너

| 배너 | 뜻 |
|---|---|
| 🟩 `✅ [이름] 구간 2/5 · 451~950 · 준비완료` | 프리셋과 화면이 일치. 시작 버튼을 누르면 된다 |
| 🟨 `⏳ … N초 후 자동 시작합니다` | 자동시작 카운트다운. `자동 시작 취소` 버튼이 함께 뜬다 |
| 🟨 `⏰ … HH:MM에 자동으로 시작합니다` | **예약 대기 중.** 남은 시간이 같이 뜬다 (탭 제목 `[예약]`) |
| 🟩 `▶ … 예약 시각(HH:MM)에 시작했습니다` | 예약대로 시작함 (탭 제목 `[진행중]`) |
| 🟥 `⛔ … 예약을 실행하지 않았습니다` | `예약 취소`를 눌렀거나 시작 직전 대조 실패. 사유가 적힌다 (탭 제목 `[예약취소]`) |
| 🟩 `▶ … 자동 시작했습니다` | 더망고가 실제로 배치를 시작한 것을 확인함 |
| 🟥 `⛔ 설정이 프리셋과 다릅니다` | **시작 버튼이 잠긴다.** 어긋난 항목이 배너에 나열된다 |
| 🟥 `⛔ … 자동 시작을 취소했습니다` | 2-5의 관문 중 하나에 걸림. 사유가 배너에 적힌다 |
| 🟥 `🏁 … (처리 0건 / 대상 N건)` | **한 건도 처리하지 않고 끝남.** 탭 제목 `[완료·0건]` / 대기 중이면 `[대기·0건]`. 3-3-3 참고 |
| 🟨 `⚠ 런처 작업 정보를 찾지 못했습니다` | 브라우저 저장소가 지워짐. 그 창은 수동 확인 후 사용 |
| 🟦 `🏁 … 이 구간이 끝났습니다` | 완료. 반복이 없으면 여기서 끝 (탭 제목 `[완료]`) |
| 🟦 `🔁 … N회차 완료. 매일 HH:MM에 다시 시작합니다` | 반복 대기 중. 남은 시간이 같이 뜬다 (탭 제목 `[대기]`) |
| 🟩 `▶ … N회차를 시작했습니다` | 런처가 반복 회차를 시작함 (탭 제목 `[진행중]`) |
| ⬛ `⏹ … 반복을 멈췄습니다` | `반복 중지`를 눌렀거나 대조 실패. 사유가 적힌다 (탭 제목 `[반복중지]`) |

탭 제목만 봐도 창 상태를 알 수 있다 — `[예약]` / `[진행중]` / `[대기]` / `[완료]` / `[반복중지]` / `[예약취소]`.

범위를 쓰는 창에서는 `검색결과모든상품` 시작 버튼도 함께 잠근다. **범위설정 쪽 시작 버튼**을 쓰면 된다.

> **v1.3에서 이 잠금을 고쳤다.** v1.2는 `onclick` 속성만 덮어썼는데, 더망고가 배치를 마치고 부르는 `update_btn_change('on')`이 `onclick="start_ini_all();"`을 **다시 심어 잠금이 조용히 풀렸다.** 실측으로 확인한 버그다. v1.3은 이벤트 capture 단계에서 잘라 복원과 무관하게 잠금이 유지된다.

구간 번호(`2/5`)는 **실제로 열린 창** 기준이다. 8구간 중 5개를 골랐으면 `1/5 … 5/5`로 매겨진다.

### 3-4-1. 자동시작 + `auto_repeat` 조합 — v1.4에서 해소됨

v1.3까지는 자동시작이 "사람이 화면을 안 보는 시간"을 늘리는데 더망고는 배치가 끝나면 스스로 재시작해서, **아무도 모르는 사이 같은 구간이 여러 겹으로 도는** 문제가 있었다. 실제로 이 조합에서 사고가 났다(3-3).

v1.4는 창이 열리는 즉시 반복을 끄므로 이 조합 주의는 더 이상 필요 없다. 다만 배너에 `🔒`가 떴는지는 한 번 확인하는 게 좋다. 반복이 필요하면 3-3-1의 런처 반복을 쓴다.

**자식 창을 새로고침해도 다시 자동시작하지 않는다.** v1.3까지는 `#tmglauncher=` 해시와 저장된 작업 정보가 리로드를 넘어 살아남아, F5 한 번에 확인창 없이 또 자동시작했다. v1.4는 자동시작 플래그를 **1회용**으로 만든다(새로고침한 창은 준비완료 상태로 뜨고, 시작은 사람이 누른다).

### 3-5. ★ 변동일(`ps_chd`)이 어떻게 "변동"을 아는가

**더망고 자체 기능이다. 실시간 조회가 아니라 더망고 DB에 쌓인 기록을 읽는 것이다.**

더망고는 상품마다 **가격/재고변동일자** 필드를 들고 있다(같은 화면의 날짜 조회 드롭다운에도 `등록일자 / 업데이트일자 / 가격/재고변동일자` 세 가지가 있다). 이 필드는 **상품 업데이트가 돌면서 원문(수집사이트)을 긁어와 값이 달라진 것을 발견했을 때** 갱신된다.

여기서 중요한 순서가 나온다.

```
① 업데이트 실행 (수집사이트 → 더망고)  → 변동을 "발견"하고 날짜를 기록
② 변동일 필터로 전송 (더망고 → 마켓)   → ①이 찾아낸 것만 골라 반영
```

**②만 돌리면 아무것도 안 잡힌다.** 변동일은 ①의 결과물이지 원문을 실시간으로 보는 게 아니기 때문이다. 아직 업데이트를 돌리지 않은 상품은 원문 가격이 어제 바뀌었어도 변동일에 걸리지 않는다.

그래서 매일 운용은 이 두 프리셋으로 나누는 게 자연스럽다.

| | 프리셋 A (발견) | 프리셋 B (반영) |
|---|---|---|
| 업데이트 항목 | 모든정보 | **비움** |
| 전송 마켓 | **비움** | 옥션·G마켓 등 |
| 변동일 | 전체 | **최근 1일** |
| 범위 | 구간 분할 (창 5개까지 효과 있음) | 대상이 적으므로 분할 불필요 |

A를 먼저 완주시키고 B를 돌리면, 9,000건 전량을 마켓에 밀어 넣지 않고 **바뀐 것만** 반영할 수 있다.

### 3-6. ★ 범위 실행이냐 전량 실행이냐는 **버튼이 정하지 않는다** (2026-08-01 실측)

시작 버튼 두 개는 **인자 없는 같은 함수 `start_ini_all()`** 을 호출한다. 어느 버튼을 눌렀는지는 전달되지 않는다.

실제 판정은 **범위 입력 영역(`#sp_limit_info`)이 열려 있는가** 하나로만 이뤄진다.

```
범위 영역 열림 → start_limit ~ end_limit 만 실행
범위 영역 닫힘 → 검색결과 전량 실행   ← 위험
```

그래서 자동시작은 클릭 직전에 **범위 영역이 열려 있는지**를 반드시 확인한다(2-5의 6번 관문). 이 검사가 없으면 범위 UI가 어떤 이유로 닫혔을 때 조용히 전량이 돈다.

거꾸로, 범위 창에서 `검색결과모든상품` 버튼을 눌러도 범위 영역이 열려 있는 한 **범위만 실행된다.** 버튼 잠금은 표시를 명확히 하려고 유지하는 것이다.

### 3-7. 자동시작이 더망고의 거부 메시지를 삼키지 않는가

`start_ini_all()`에는 거부용 `alert`가 7개 있다(로딩중 / 범위 미입력 / 범위 순서 역전 / 사이트·마켓 없음 / 사이트 미선택 / 항목·마켓 미선택 / 확장 사이트 다중선택). `confirm`은 없다.

자동 클릭은 이 `alert`를 잠깐 가로채 **메시지를 배너에 그대로 옮겨 적는다.** 모달로 창이 멈추는 것을 막으면서도 내용은 남긴다. 가로채기는 클릭 순간에만 걸고 즉시 원복하므로, 이후 비동기로 뜨는 마켓 로그인 오류 등은 평소처럼 팝업으로 뜬다.

### 3-8. ★ v1.3.0 자동시작 버그 — `window.isLoaded`는 영원히 `undefined`다

**증상**: `실행+자동시작`으로 연 창이 30초 동안 `⏳ 페이지 로딩 대기 중`만 띄우다가 취소됐다. 그 사이 배너를 클릭한 사용자에겐 "배너를 눌렀더니 취소됐다"로 보였다. 두 증상은 같은 원인이다.

**원인**: `start_ini_all()`의 첫 관문인 로딩 플래그가 더망고 페이지에 **`let isLoaded`** 로 선언돼 있다.

```
var  x = 1  →  window.x 로 읽힌다
let  x = 1  →  window.x 는 undefined  ← 전역 렉시컬 선언은 window에 붙지 않는다
```

v1.3.0은 `window.isLoaded !== true`를 조건으로 기다렸다. 이 값은 **끝내 `undefined`**라 조건이 영원히 참이고, 30초 타임아웃에 걸려 매번 취소됐다. 실제 플래그는 창이 열리고 **약 0.8초 뒤** `true`가 된다.

**수정(v1.3.1)**

- 플래그는 `typeof isLoaded`로 **맨이름으로** 읽는다(`pageLoadedFlag()`). 읽을 수 없으면 `null`을 돌려주고, 그때는 **이 플래그로 판정하지 않는다.**
- 로딩 여부의 최종 판정은 **더망고 자신**에게 맡긴다 — 일단 눌러 보고 `페이지 로딩중입니다` alert가 오면 재시도한다. 페이지가 변수 이름·선언 방식을 바꿔도 깨지지 않는다.

> 교훈: **페이지 내부 변수를 `window.…`로 읽기 전에 `var`인지 `let`인지 확인할 것.** 그리고 가능하면 내부 변수 대신 **페이지가 내놓는 응답**으로 판정할 것.

> v1.0에서 변동일·상품상태 드롭다운이 비어 보였던 건 버그였다. 페이지 HTML이 깨져 있어 두 `<select>`가 `<form>` **바깥**에 놓이는데 CSS 자손 선택자로 찾으려 해서 실패했다. v1.1에서 `form.elements` 접근으로 고쳤다.

---

## 4. 처음 도입할 때 — 파일럿 순서

바로 전량을 돌리지 말 것. 잘못된 마켓에 나간 전송은 되돌리기 어렵다.

1. **파일럿 A** — 프리셋 하나, 구간 하나만 체크, 총건수를 `5`로 임시 설정 → 창 1\~2개로 실제 시작
   → `전송로그 보기`에서 **의도한 마켓·의도한 5건만** 처리됐는지 확인
2. **파일럿 B** — 같은 사이트로 6창을 일부러 열어 본다
   → 5창 초과 시 서버가 어떻게 반응하는지(거부/대기/조용한 실패) 확인하고 결과를 기록
3. 이상 없으면 실제 구간으로 전환

**중단 조건:** 파일럿 A에서 의도하지 않은 마켓에 전송되면 즉시 중단하고 원인부터 규명한다.

---

## 5. 알려진 제약

- 범위(`start_limit`/`end_limit`)는 URL로 전달되지 않아 **창이 열린 뒤** 스크립트가 채워 넣는다. 창 로딩이 끝나기 전에 시작을 누르면 범위가 비어 있을 수 있다 — **초록 배너가 뜬 뒤에** 누를 것.
- 프리셋은 이 브라우저에만 저장된다. 다른 PC로 옮기려면 다시 만들어야 한다.
- 여러 사이트를 한 프리셋에 넣는 기능은 넣지 않았다. 화면이 사이트 순서를 고정하고, 한도도 사이트별이라 프리셋을 나누는 편이 명확하다.
