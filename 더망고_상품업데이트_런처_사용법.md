# 더망고 상품업데이트 런처 — 사용법

`더망고_상품업데이트_런처.user.js` · v1.2 (2026-08-01)

매일 반복하는 **상품업데이트 & 마켓전송** 설정을 프리셋으로 저장해 두고, 상품 범위를 구간으로 나눠 **여러 창을 한 번에** 띄운다.

> **시작 버튼은 사람이 누른다.** 런처는 창을 열고 설정을 대조하는 데까지만 관여한다.

---

## 1. 설치

1. Tampermonkey → 새 스크립트 → `더망고_상품업데이트_런처.user.js` 내용 붙여넣기 → 저장
2. 크롬에서 이 사이트의 **팝업 허용**을 켠다 (주소창 오른쪽 아이콘 → 항상 허용). 안 켜면 첫 창 하나만 열리고 멈춘다.
3. 더망고 **로그인 + 크롬 확장 설치** 상태여야 업데이트가 실제로 돈다. 확장이 없으면 시작을 눌러도 진행되지 않는다.

설치 후 `상품업데이트 & 마켓전송` 화면 오른쪽 위에 **상품업데이트 런처** 패널이 뜬다.

---

## 2. 기본 흐름

```
프리셋 만들기 → 건수 조회 → 구간 확인 → 실행 → 열린 창마다 시작 버튼 클릭
                                              ↓
                                     끝나면 창을 닫는다 (중요)
```

### 2-1. 프리셋 만들기

| 항목 | 설명 |
|---|---|
| 이름 | 구분용. 예 `국내자라-옥션G마켓` |
| 수집사이트 | **하나만** 고른다. 창 한도가 사이트별로 걸리므로 프리셋도 사이트별로 나눈다 |
| 업데이트 항목 | 원문(수집사이트) → 더망고 갱신. 기본값 **가격 + 재고**. 비우면 업데이트를 건너뛰고 전송만 |
| 전송 마켓 | 더망고 → 마켓 전송. 범위 분할을 쓰면 이 값이 **구간 기본값**이 된다 |
| 변동일 | 최근 N일 안에 가격·재고가 바뀐 상품만. 아래 3-5 참고 |
| 정렬 | **상품수집 날짜순(과거순) 고정.** 아래 3-2 참고 |
| 범위 분할 | 총건수·첫구간·이후구간 → 구간 목록 자동 생성 |

프리셋을 고르지 않고 그냥 쓸 수 있다. 드롭다운 기본값이 `-- 선택 안함 --`이고, 이 상태에서도 설정을 채워 바로 `실행`하면 된다. 다시 쓸 설정만 `저장`으로 남기면 된다(브라우저 localStorage).

> **상품상태는 `판매상품(재고+품절)`으로 고정**돼 있다. 거의 바꿀 일이 없어 패널에서 뺐다.

**업데이트 항목 — `모든정보`는 배타적이다.** `모든정보`를 켜면 나머지 4개가 꺼지고, 나머지 중 하나를 켜면 `모든정보`가 꺼진다. 화면 동작과 같다.

> 강의 권고: 가격·재고만 업데이트하면 **현재 수집된 옵션에 대해서만** 갱신되고 새 옵션은 안 들어온다. 속도는 항목별로 동일하므로 특별한 경우가 아니면 `모든정보`를 쓰는 편이 안전하다.

### 2-2. 건수 조회

`건수 조회` 버튼은 **현재 화면이 아니라 프리셋 조건 그대로** 서버에 조회해 건수를 가져온다.
화면 조건과 프리셋이 달라도 틀린 구간이 만들어지지 않는다.

상품이 늘면 이 버튼만 다시 누르면 구간이 재계산된다.

### 2-3. 구간

기본 **첫 구간 450건, 이후 500건씩**. 두 값 모두 바꿀 수 있다.

```
9,159건 → 19구간
1 ~ 450 / 451 ~ 950 / 951 ~ 1450 / … / 8951 ~ 9159
```

구간마다 체크박스가 있다. **체크 해제하면 그 구간은 열지 않는다** — 실패한 구간만 다시 돌리거나, 앞쪽 몇 개만 먼저 돌릴 때 쓴다. 목록 위의 `전체선택` / `전체해제`로 한 번에 처리할 수도 있다.

체크박스를 연속으로 눌러도 목록이 다시 그려지지 않으므로 **스크롤 위치가 그대로 유지된다.**

### 2-3-1. 구간별 전송 마켓

구간 줄 오른쪽의 `[옥션2.0, G마켓2.0 ▾]`을 누르면 **그 구간만의 마켓**을 고를 수 있다.

```
[x] 1. 1~450     [옥션2.0, G마켓2.0 ▾]
[x] 2. 451~950   [옥션2.0 ▾]          ← 이 구간만 옥션
[x] 3. 951~1450  [옥션2.0, G마켓2.0 ▾]
```

- 손대지 않은 구간은 위쪽 **전송 마켓(구간 기본값)** 을 따른다.
- 따로 지정한 구간은 링크가 **빨간색**으로 표시된다.
- `이 마켓을 전 구간에 적용`을 누르면 그 구간의 마켓이 기본값이 되고 개별 지정은 모두 해제된다.

### 2-4. 실행

`실행 (N창)` → 선택한 구간을 **전부** 1.2초 간격으로 연다. 창 수 제한이나 확인창은 없다.

팝업이 차단되면 그 지점에서 멈추고 `남은 N개 열기` 버튼이 나타난다. 팝업을 허용한 뒤 그 버튼을 누르면 이어서 연다.

> 강의 기준 권장 창 수(업데이트 전용 5창 / 전송 포함 1\~3창)는 **열린 창의 배너**에 안내로만 표시된다. 막지 않는다.

---

## 3. 알아둘 것

### 3-1. 창을 몇 개 띄울지는 조합에 따라 다르다 (강의 3-24, 3-35)

런처는 막지 않는다. 아래는 판단 기준이다.

| 프리셋 조합 | 권장 | 왜 |
|---|---|---|
| 업데이트만 (마켓 미선택) | 사이트당 **5** | 리소스를 나누지 않아 **창 수만큼 빨라진다** |
| 전송만 (항목 미선택) | **1** 권장 | *"3개 띄운다고 3배속이 안 돼요. 리소스를 3개로 나눠요"* — 나눠도 효과가 거의 없다 |
| 스마트스토어 단독 전송 | **3** | 과속하면 덮어쓰기 오류가 난다 |
| 업데이트 + 전송 | 3 | 전송 쪽이 병목 |

**구간을 잘게 나눠 이득을 보는 건 "업데이트만" 프리셋이다.** 전송이 섞인 프리셋을 10개로 쪼개도 빨라지지 않는다.

### 3-2. 정렬은 과거순 고정

`상품수집 날짜순(과거순)` = uid 기준이라 회차마다 순서가 흔들리지 않는다.
날짜순(상품업데이트 날짜순 등)으로 정렬하면 **오늘의 1\~450번과 내일의 1\~450번이 다른 상품**이 되어 구간이 의미를 잃는다. 그래서 선택지로 열어 두지 않았다.

### 3-3. 창은 끝나면 닫아야 한다 — 재시작하는 건 **더망고**다

**이 반복은 더망고 기능이지 Tampermonkey나 이 런처와는 무관하다.**

`admin_goods_update.php`를 열면 더망고 서버가 페이지 안에 `auto_repeat = "Y"`를 직접 심어 보낸다(계정 설정값). 배치가 끝나면 더망고의 `auto_restart_ini()`가 0\~60분 랜덤 대기 후 **스스로 다시 시작한다.** 런처를 지워도 똑같이 반복한다.

범위를 고정해 뒀으므로 닫지 않으면 같은 450\~500건을 계속 돈다. 배너에도 경고가 뜬다.
반복을 끄려면 더망고 **환경설정**에서 자동반복 값을 바꿔야 한다(이 화면에는 토글이 없다).

### 3-4. 열린 창의 배너

| 배너 | 뜻 |
|---|---|
| 🟩 `✅ [이름] 구간 2/5 · 451~950 · 준비완료` | 프리셋과 화면이 일치. 시작 버튼을 누르면 된다 |
| 🟥 `⛔ 설정이 프리셋과 다릅니다` | **시작 버튼이 잠긴다.** 어긋난 항목이 배너에 나열된다 |
| 🟨 `⚠ 런처 작업 정보를 찾지 못했습니다` | 브라우저 저장소가 지워짐. 그 창은 수동 확인 후 사용 |

범위를 쓰는 창에서는 `검색결과모든상품` 시작 버튼도 함께 잠근다 — 잘못 눌러 전량이 도는 것을 막기 위해서다. **범위설정 쪽 시작 버튼**을 쓰면 된다.

구간 번호(`2/5`)는 **실제로 열린 창** 기준이다. 8구간 중 5개를 골랐으면 `1/5 … 5/5`로 매겨진다.

### 3-5. ★ 변동일(`ps_chd`)이 어떻게 "변동"을 아는가

**더망고 자체 기능이다. 실시간 조회가 아니라 더망고 DB에 쌓인 기록을 읽는 것이다.**

더망고는 상품마다 **가격/재고변동일자** 필드를 들고 있다(같은 화면의 날짜 조회 드롭다운에도 `등록일자 / 업데이트일자 / 가격/재고변동일자` 세 가지가 있다). 이 필드는 **상품 업데이트가 돌면서 원문(수집사이트)을 긁어와 값이 달라진 것을 발견했을 때** 갱신된다.

여기서 중요한 순서가 나온다.

```
① 업데이트 실행 (수집사이트 → 더망고)  → 변동을 "발견"하고 날짜를 기록
② 변동일 필터로 전송 (더망고 → 마켓)   → ①이 찾아낸 것만 골라 반영
```

**②만 돌리면 아무것도 안 잡힌다.** 변동일은 ①의 결과물이지 원문을 실시간으로 보는 게 아니기 때문이다. 아직 업데이트를 돌리지 않은 상품은 원문 가격이 어제 바뀌었어도 변동일에 걸리지 않는다.

그래서 매일 운용은 이 두 프리셋으로 나누는 게 자연스럽다.

| | 프리셋 A (발견) | 프리셋 B (반영) |
|---|---|---|
| 업데이트 항목 | 모든정보 | **비움** |
| 전송 마켓 | **비움** | 옥션·G마켓 등 |
| 변동일 | 전체 | **최근 1일** |
| 범위 | 구간 분할 (창 5개까지 효과 있음) | 대상이 적으므로 분할 불필요 |

A를 먼저 완주시키고 B를 돌리면, 9,000건 전량을 마켓에 밀어 넣지 않고 **바뀐 것만** 반영할 수 있다.

> v1.0에서 변동일·상품상태 드롭다운이 비어 보였던 건 버그였다. 페이지 HTML이 깨져 있어 두 `<select>`가 `<form>` **바깥**에 놓이는데 CSS 자손 선택자로 찾으려 해서 실패했다. v1.1에서 `form.elements` 접근으로 고쳤다.

---

## 4. 처음 도입할 때 — 파일럿 순서

바로 전량을 돌리지 말 것. 잘못된 마켓에 나간 전송은 되돌리기 어렵다.

1. **파일럿 A** — 프리셋 하나, 구간 하나만 체크, 총건수를 `5`로 임시 설정 → 창 1\~2개로 실제 시작
   → `전송로그 보기`에서 **의도한 마켓·의도한 5건만** 처리됐는지 확인
2. **파일럿 B** — 같은 사이트로 6창을 일부러 열어 본다
   → 5창 초과 시 서버가 어떻게 반응하는지(거부/대기/조용한 실패) 확인하고 결과를 기록
3. 이상 없으면 실제 구간으로 전환

**중단 조건:** 파일럿 A에서 의도하지 않은 마켓에 전송되면 즉시 중단하고 원인부터 규명한다.

---

## 5. 알려진 제약

- 범위(`start_limit`/`end_limit`)는 URL로 전달되지 않아 **창이 열린 뒤** 스크립트가 채워 넣는다. 창 로딩이 끝나기 전에 시작을 누르면 범위가 비어 있을 수 있다 — **초록 배너가 뜬 뒤에** 누를 것.
- 프리셋은 이 브라우저에만 저장된다. 다른 PC로 옮기려면 다시 만들어야 한다.
- 여러 사이트를 한 프리셋에 넣는 기능은 넣지 않았다. 화면이 사이트 순서를 고정하고, 한도도 사이트별이라 프리셋을 나누는 편이 명확하다.
